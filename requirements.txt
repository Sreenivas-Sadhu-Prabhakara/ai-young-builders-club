# Build the AI Young Builders Club site locally or in CI.
#   python3 -m venv .venv
#   ./.venv/bin/pip install -r requirements.txt
#   ./.venv/bin/python -m mkdocs serve
mkdocs-material>=9.5
pymdown-extensions>=10.7
